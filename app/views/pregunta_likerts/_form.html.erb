<%= form_for(@pregunta_likert) do |f| %>
  <% if @pregunta_likert.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@pregunta_likert.errors.count, "error") %> prohibited this pregunta_likert from being saved:</h2>

      <ul>
      <% @pregunta_likert.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="field">
    <%= f.label :pregunta %><br>
    <%= f.text_area :pregunta %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
